# Runtime bundles are deterministic generated source. Keep their wrapper bytes
# stable across Windows and Unix checkouts so the canonical integrity gate works.
lib/generated/looplab-*-browser-bundle.mjs text eol=lf
